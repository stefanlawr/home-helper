{
  "name": "home-helper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run check:data && npm run check:logic && npm run check:api",
    "preview": "vite preview",
    "check:data": "node scripts/validate-data.mjs",
    "check:api": "node scripts/validate-api-cache.mjs",
    "check:logic": "node scripts/validate-logic.mjs"
  },
  "dependencies": {
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "vite": "^8.2.0"
  }
}
