{
  "name": "home-helper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run check:data",
    "preview": "vite preview",
    "check:data": "node scripts/validate-data.mjs",
    "check:api": "node scripts/validate-api-cache.mjs"
  },
  "dependencies": {
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "vite": "^8.2.0"
  }
}
